<?xml version="1.0" encoding="UTF-8"?>
<?rfc toc="yes"?>
<?rfc tocompact="yes"?>
<?rfc tocdepth="4"?>
<?rfc tocindent="yes"?>
<?rfc symrefs="yes"?>
<?rfc sortrefs="yes"?>
<?rfc comments="yes"?>
<?rfc inline="yes"?>
<?rfc compact="yes"?>
<?rfc subcompact="no"?>
<rfc xmlns:xi="http://www.w3.org/2001/XInclude" category="std" ipr="trust200902" docName="draft-ietf-grow-bmp-yang-latest" submissionType="IETF">
  <front>
    <title abbrev="BMP YANG Data Model">A YANG Data Model for BMP</title>
    <author fullname="Camilo Cardona" initials="C" surname="Cardona ">
      <organization>NTT</organization>
      <address>
        <postal>
          <street>164-168, Carrer de Numancia</street>
          <city>Barcelona</city>
          <code>08029</code>
          <country>Spain</country>
        </postal>
        <email>camilo@ntt.net</email>
      </address>
    </author>
    <author fullname="Paolo Lucente" initials="P." surname="Lucente">
      <organization>NTT</organization>
      <address>
        <postal>
          <street>Siriusdreef 70-72</street>
          <city>Hoofddorp</city>
          <code>2132</code>
          <country>Netherlands</country>
        </postal>
        <email>paolo@ntt.net</email>
      </address>
    </author>
    <author fullname="Thomas Graf" initials="T." surname="Graf">
      <organization>Swisscom</organization>
      <address>
        <postal>
          <street>Binzring 17</street>
          <city>Zurich 8045</city>
          <country>Switzerland</country>
        </postal>
        <email>thomas.graf@swisscom.com</email>
      </address>
    </author>
    <author fullname="Benoit Claise" initials="B" surname="Claise">
      <organization>Everything OPS</organization>
      <address>
        <email>Benoit@everything-ops.net</email>
      </address>
    </author>
    <author fullname="Dhananjay Patki" initials="D" surname="Patki">
      <organization>Cisco</organization>
      <address>
        <postal>
          <street>Cessna Business Park SEZ, Kadubeesanahalli</street>
          <city>Bangalore</city>
          <country>India</country>
        </postal>
        <email>dhpatki@cisco.com</email>
      </address>
    </author>
    <author fullname="Prasad S. Narasimha" initials="P" surname="Narasimha">
      <organization>Cisco</organization>
      <address>
        <postal>
          <street>Cessna Business Park SEZ, Kadubeesanahalli</street>
          <city>Bangalore</city>
          <country>India</country>
        </postal>
        <email>snprasad@cisco.com</email>
      </address>
    </author>
    <date/>
    <area>OPS</area>
    <workgroup>GROW</workgroup>
    <abstract>
      <t> 
        This document defines a YANG data model for the configuration and monitoring of the BGP Monitoring Protocol (BMP).
        The data model covers the base BMP protocol defined in <xref
        target="RFC7854"/> and includes support for the Loc-RIB extension
        defined in <xref target="RFC9069"/>.
      </t>
    </abstract>
  </front>
  <middle>
    <section title="Introduction">
      <t>
            The BGP Monitoring Protocol (BMP) provides a mechanism for
          monitoring BGP sessions.  This document defines a YANG data
          model for BMP, specified in two YANG modules, enabling
          operators to configure and manage BMP sessions; control the
          data exported to monitoring stations; and collect statistics
          for each BMP session.  The data model is designed to
          accommodate both simple and advanced deployment scenarios,
          supporting granular control over network instances, RIBs,
          address families, and peers.
        </t>
        <t>
            The data model defined in this document supports the base
            BMP protocol defined in <xref target="RFC7854"/>.  It also
            supports the Loc-RIB extension defined in <xref
                target="RFC9069"/>, allowing configuration and
            monitoring of BMP reporting for routes selected into the
            local BGP speaker's Loc-RIB.
        </t>
    </section>
    <section title="Terminology" anchor="terminology">
      <t>
         The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL
         NOT", "SHOULD", "SHOULD NOT", "RECOMMENDED", "NOT RECOMMENDED",
         "MAY", and "OPTIONAL" in this document are to be interpreted as
         described in BCP 14 <xref target="RFC2119"/> <xref target="RFC8174"/>
         when, and only when, they appear in all capitals, as shown here.
      </t>
      <t>
           Routing Information Bases, peers, monitoring stations, and
           initiation messages are defined in <xref target="RFC7854"/>.
      </t>
    </section>
    <section title="Data Model Description" anchor="model_description">
      <t>
              This document specifies two YANG modules. The main module, ietf-bmp, configures and
          monitors the BGP Monitoring Protocol (BMP)  <xref target="RFC7854"/> on the monitored router.  
          A second auxiliary module, ietf-bmp-tcp-dependencies, provides extensions for configuring the TCP configuration
          to BMP monitoring stations if the BMP server supports the groupings from <xref target="RFC9643"/>.
          The main module provides parameters for configuring the session to BMP monitoring
              stations; configuration of BMP messages; provides operational
              metrics and enables resetting of BMP monitoring sessions.
       </t>
      <t>The main module is included in <xref target="ietf-bmp"/>. The full tree diagram
           is available in <xref target="ietf-bmp-tree-view"/>. This
               section provides details and examples of each part of the main module.
          The secondary TCP extension module augments the definition of TCP configuration
          for a monitoring station, and it is explained in section <xref target="tcp_options"/>.</t>
      <t>The ietf-bmp YANG module is placed at the root of the YANG tree.  At its
           upper level, the module lists each monitoring station. Every
           monitoring station is identified by an ID, which is a string
           provided by the operator. <xref target="tree_overview"/> shows 
           the high-level structure of the module.</t>
      <figure anchor="tree_overview">
        <name>High-level structure of the ietf-bmp module</name>
        <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(trees/tree-overview.txt)
]]></sourcecode>
      </figure>
      <section title="Connection" anchor="connection_section">
        <t>The connection container configures how the monitored router connects to a BMP monitoring station.
            This includes IP connectivity options, TCP parameters, and session timing. 
            <xref target="tree_connection"/> shows the structure of the connection container.</t>
        <figure anchor="tree_connection">
          <name>Structure of the connection container</name>
          <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(trees/tree-connection.txt)
]]></sourcecode>
        </figure>
        <t>The rest of the subsections describe  each of the containers use to configure each monitoring-station.</t>
        <section title="IP Connectivity" anchor="ip_connectivity">
        <t> BMP allows for active and passive
            connections between the router and the BMP monitoring station as described in  <xref target="RFC7854" section="3.2" sectionFormat="of"/>. In
            an active connection, the router establishes the TCP connection to
            the monitoring station, while in a passive one, it is the monitoring
            station which initiates the connection. The ietf-bmp module
            provides options for both types of connection using a choice.</t>
        <t>The following subsections describe each type of connection option, with 
            examples of their configuration.</t>
        <section title="Active connection">
          <t>For an active connection, the IP address and port of the monitoring
          station, together with the local endpoint must be provided. The local
            endpoint can be a local IP address or a source interface. One can
            optionally provide the local port for establishing the connection.
            If the monitoring station is connected over a network-instance
            instead of the global one, this one must also be specified. An
            example of configuration is included in <xref target="active_connection_option"/>.</t>
          <figure anchor="active_connection_option">
            <name>Active connection example</name>
            <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_active_connection_option.xml)
           ]]></sourcecode>
          </figure>
          <t>Note in the example from  <xref target="active_connection_option"/>
                that there is no network instance defined, so the connection is
                using the global network instance.</t>
        </section>
        <section title="Passive connection">
          <t>In a passive connection, the IP of the monitoring station, the local endpoint, and the local port
            for the incoming connection must be specified. If the port of the
            monitoring station is provided, it must match the incoming
            connection. If the monitoring station is connected through a
            network-instance instead of the global one, this one must also be
            specified.</t>
          <t>An incoming connection not matching a valid entry MUST be ignored by
            the router.</t>
          <t><xref target="passive_connection_option"/> includes an example of
            configuring a passive connection. In this example, the
            network-instance where the connection is received is specified.</t>
          <figure anchor="passive_connection_option">
            <name>Passive connection example</name>
            <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_passive_connection_option.xml)
           ]]></sourcecode>
          </figure>
        </section>
      </section>
        <section title="TCP Options" anchor="tcp_options">
        <t>The ietf-bmp module, together with the ietf-bmp-tcp-dependencies module, allows tuning various parameters of the TCP
            connection supporting the BMP session:</t>
        <t>
          <ul spacing="compact">
            <li>The maximum segment size of the TCP connection. See  <xref target="RFC9293" section="3.7.1" sectionFormat="of"/>.</li>
            <li>Enabling MTU discovery for the path. See <xref target="RFC9293" section="3.7.2" sectionFormat="of"/>.</li>
            <li>For configuring TCP keepalives, the connection container uses the tcp-common-grouping from <xref target="RFC9643"/>. Note that the implementation must support module ietf-bmp-tcp-dependencies in addition to the main module to support these elements.  Please see <xref target="RFC9643" section="2.1.3.1" sectionFormat="of"/> for the explanation of each of its parameters. The device MUST have the feature "tcp-client-keepalives" enabled. See also <xref target="RFC9293" section="3.8.4" sectionFormat="of"/></li>
            <li>Session security. Provides options for authentication using the TCP Authentication Option (TCP-AO) <xref target="RFC5925"/> and MD5. This feature also requires support of the ietf-bmp-tcp-dependencies module.</li>
          </ul>
        </t>
        <t>Figures <xref target="tcp_example1" format="counter"/> and <xref target="tcp_example2" format="counter"/> include examples configuring the previous TCP parameters in the model.</t>
        <t>
          <figure anchor="tcp_example1">
            <name>Example of configuring basic TCP parameters</name>
            <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_tcp_example1.xml)
           ]]></sourcecode>
          </figure>
        </t>
        <t>
          <figure anchor="tcp_example2">
            <name>Example of the Configuration of TCP session security.</name>
            <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_tcp_example2.xml)
           ]]></sourcecode>
          </figure>
        </t>
      </section>
        <section title="Other BMP connectivity options">
        <t>The ietf-bmp module also includes the following options to configure the connection to the BMP monitoring station:</t>
        <t>
          <ul spacing="compact">
            <li>Initial-delay: a value in seconds that the device must wait before starting the connection to the station. An operator can use this delay to let BGP converge before starting the BMP session.</li>
            <li>Backoff time: Configuration of the backoff time strategy after failing to connect to the monitoring station. The module includes a basic exponential backoff with a default initial backoff of 30 seconds and a maximum of 720 seconds, as suggested in <xref target="RFC7854" section="3.2" sectionFormat="of"/>.</li>
          </ul>
        </t>
        <t>The example in <xref target="example_indelay"/> configures an initial-delay of 10, an initial-backoff of 50 seconds and 600 of maximum-backoff.</t>
        <t>
          <figure anchor="example_indelay">
            <name>Example of the initial-delay and simple exponential backoff.</name>
            <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_example_indelay.xml)
           ]]></sourcecode>
          </figure>
        </t>
        </section>
      </section>
      <section title="BMP data" anchor="bmp_data_section">
        <t>The bmp-data container defines the configuration parameters for the data that the device sends to the monitoring station using BMP messages. See <xref target="RFC7854" section="4" sectionFormat="of"/>.
            <xref target="tree_bmp_data"/> shows the structure of the bmp-data container.</t>
        <figure anchor="tree_bmp_data">
          <name>Structure of the bmp-data container</name>
          <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(trees/tree-bmp-data.txt)
]]></sourcecode>
        </figure>
        <t>The ietf-bmp module defines options for the initiation message, the statistics report, the route mirroring, and the route monitoring. The first three have simple configuration options and are described next. Route monitoring is the most complex and is detailed in <xref target="route_monitoring"/>.</t>
        <t>
          <ul spacing="compact">
            <li>Initiation-message: Content for an information TLV type-0 for identification of the device. See <xref target="RFC7854" section="4.3" sectionFormat="bare"/> and <xref target="RFC7854" section="4.4" sectionFormat="of"/></li>
            <li>Statistics-interval: The statistics report is enabled by the presence of the statistics-report container. The statistics-interval is mandatory if the statistics-report container exists and defines the interval of the statistics report. See <xref target="RFC7854" section="4.8" sectionFormat="of"/>.</li>
            <li>Route-mirroring: Route Mirroring messages serve as an exact replica of the messages received by the device. See <xref target="RFC7854" section="6" sectionFormat="of"/>. Enabling route mirroring messages towards a particular BMP monitoring station only requires the presence of the container "route-mirroring" within the monitoring station container.</li>
          </ul>
        </t>
        <t>An example of configuring the previous options is included in <xref target="example_init_msg"/></t>
        <t>
          <figure anchor="example_init_msg">
            <name>Example of configuration of initiation-message and statistics report interval.</name>
            <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_example_init_msg.xml)
           ]]></sourcecode>
          </figure>
        </t>
        <section title="BMP route monitoring" anchor="route_monitoring">
            <t>Route monitoring messages are used for synchronization of RIBs to the monitoring station. See <xref target="RFC7854" section="5" sectionFormat="of"/>. 
              The Loc-RIB route monitoring support in this data model follows the Loc-RIB extension defined in <xref target="RFC9069"/>.
              <xref target="tree_route_monitoring"/> shows the detailed structure of the route-monitoring container.</t>
          <figure anchor="tree_route_monitoring">
            <name>Structure of the route-monitoring container</name>
            <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(trees/tree-route-monitoring.txt)
]]></sourcecode>
          </figure>
          <t>The following requirements guided the design of this part of the ietf-bmp module:</t>
          <t>
            <ul spacing="compact">
              <li>Operators might not want to receive all routes from all RIBs in a network device. For instance, some devices contain a considerable amount of data that might overwhelm the monitoring station. In these cases, operators might want to only collect information from an arbitrary subset of RIBs, address families, peers.</li>
              <li>Operators might want to configure the route monitoring messages for different network instances differently. For example, they might want to receive different address families from the global network instance than in L3 VPN network instances. </li>
              <li>In contrast to the previous points, some operators might want a simple configuration that covers multiple cases (e.g. same config for all peers, or same config for all network instances).  This would not only make configurations look smaller and concise, but will reduce the need for reconfiguring devices when you add a new peer or add a new network instance (which happens frequently on some type of networks). </li>
            </ul>
          </t>
          <t>Based on the previous points, the ietf-bmp module is designed to flexibly control the data sent through the BMP route monitoring packets, yet it provides options to facilitate configurations for simple cases, such as when the operator wants to receive all routes from a RIB.</t>
          <t>The route monitoring configuration is divided in a four-part hierarchy:</t>
          <t>
            <ul spacing="compact">
              <li>Network Instance </li>
              <li>RIB Type (e.g. Adj-RIB-IN pre/post, local RIB)</li>
              <li>Address Family </li>
              <li>Peers</li>
            </ul>
          </t>
          <t>Absence of the route monitoring container will disable the route monitoring messages to the monitoring station.</t>
          <t>This section introduces these hierarchies before describing them in detail.</t>
          <t>The number of RIB types (e.g. Adj-RIB-IN/OUT, local RIB, etc) and Address families is low, and their configuration should not change frequently. Therefore, they are configured explicitly in the model. That is, the model does not provide a way of providing a default configuration for these or configuring them in groups.</t>
          <t>On the other hand, Network instances and peers require greater flexibility.</t>
          <t>For network instances, the module should configure not only the "global" network instance, but also other network instances. Also, network instances can change frequently in networks with customer connecting to Virtual Private Networks. To not force operators to change configuration at every change, the module provides methods for  defining a "default" configuration for network instances. However, to provide control over the configuration, each network instance can be configured independently, if needed.</t>
          <t>A similar situation applies to peers for the Adj-RIB-IN and Adj-RIB-OUT RIBs. The module includes a way of configuring a default for all peers for simple cases, but one can provide configuration for type of peers, peergroups,  or each peer individually. </t>
          <t>The next list summarizes the requirements stated in the previous two paragraphs:</t>
          <t>For network instances:</t>
          <t>
            <ul spacing="compact">
              <li>The configuration should be simple for cases where only the "global" routing instance is enabled.</li>
              <li>The module should provide ways of configuring all Network instances (kind of a default config for any Network instance that is configured in the device).</li>
              <li>The module should provide a way of configuring network instances individually.</li>
            </ul>
          </t>
          <t>For peers:</t>
          <t>
            <ul spacing="compact">
              <li>The module should provide ways of configuring all peers, kind of a default. This would be the most common case.</li>
              <li>The module should provide ways of configuring peergroups.</li>
              <li>The module should provide ways of configuring type of peers. For instance, only send routes from eBGP peers.</li>
              <li>The module should provide ways of configuring individual peers. For instance, an operator might apply a route-policy to filter certain prefixes for a specific peer, or disable route monitoring messages for a peer that is noisy yet not important.</li>
            </ul>
          </t>
          <t>To further control the route monitoring data, the peer container includes a route-policy option in which the operator can further filter the data send to the BMP monitoring station.</t>
          <t>Each of the four hierarchies is described in the following sections.</t>
          <section title="Network instances">
            <t>The route monitoring configuration starts with the configuration of network instances. A network instance can be configured individually, or it can be configured if it matches any of the selectors from the "bmp-ni-types" identity. Each option is explained next.</t>
            <t>The ietf-bmp module currently defines three bmp-ni-types identities: "all-ni" which selects all network instances, "non-global-ni" which selects all network instances except the global one, and "global-ni", which configures the global network instance when the device does not offer an explicit name for it. The former can be used as a "default" configuration for simple cases.</t>
            <t>Network-instances are configured under the container "bmp/route-monitoring/network-instance".</t>
            <t>An empty configuration disables route monitoring messages for the selected network-instances. Operators can also use the "enable" leaf to disable explicitly the routing messages for the network instance. </t>
            <t>The route-monitoring data for a network instance can be configured by at most one element under the network-instance-configuration container.  There SHOULD  be clear rules for which element to apply to a network instance in case multiple elements can select it. The next part of the section provides rules and examples.</t>
            <t>The rules for selecting which element configures a network instance are presented next. Each point is evaluated only if the previous points do not hold.</t>
            <t>
              <ul spacing="compact">
                <li>If the name of the network instance is referenced in "network-instance-configuration/network-instances/network-instance", the network instance SHOULD be configured using this element.</li>
                <li>If the selector 'global-ni' under the "network-instance-configuration/network-instance-selectors" exist, the global network instance SHOULD be configured using this element. Note that if a vendor has a name for the global network instance, the previous step (i.e, network instance name) will take priority over using the global-ni selector.</li>
                <li>If the selector 'non-global-ni' under the "network-instance-configuration/network-instance-selectors" exist, any non-global network instance should be configured using its content.</li>
                <li>Any Network Instance not referenced by any rule above SHOULD be configured using the all-ni if one exists. If it does not exist, then the network instance is not configured (and therefore no route monitoring messages from the network instance are sent to the monitoring station).</li>
              </ul>
            </t>
            <t>Any extension of the bmp-ni-types SHOULD provide explanations of how to deal with case in which multiple elements select the same network instance. </t>
            <t>The following section provides examples of configuring the network instance level. For now, the discussion focuses on configuration using the BMP container. To focus on the network instance configuration, the configuration under each instance is masked using "Configuration X".</t>
            <t>
              <figure anchor="example_netins_one">
                <name>Examples of configuring the network instance level for Route Monitoring.</name>
                <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_example_netins_one.xml)
           ]]></sourcecode>
              </figure>
            </t>
            <t>In the example from <xref target="example_netins_one"/>, a "default" configuration (Configuration A) is applied to any network instance without any explicit configuration. The global network instance and network-instance-two get Configuration B and Configuration C, respectively. The network-instance-one instance container disables the route monitoring messages for that network instance.</t>
            <t>
              <figure anchor="simple_ni_configuration">
                <name>Example of configuring all network instances.</name>
                <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_simple_ni_configuration.xml)
           ]]></sourcecode>
              </figure>
            </t>
            <t>The example in <xref target="simple_ni_configuration"/> shows a "simple" configuration. In this case, all network instances would get "Configuration D". Note that `all-ni` would also cover the global instance.</t>
            <t>Another simple configuration would just involve configuring the global network instance. In this case, information of non-global network instances would not be sent to the monitoring station. This is depicted in <xref target="simple_gni_configuration"/></t>
            <t>
              <figure anchor="simple_gni_configuration">
                <name>Example of configuring only the global network instance.</name>
                <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_simple_gni_configuration.xml)
           ]]></sourcecode>
              </figure>
            </t>
          </section>
          <section title="RIB Type">
            <t>Each RIB type is configured explicitly in the module through a container. The ietf-bmp module currently provides containers for adj-rib-out-pre, adj-rib-out-post, adj-rib-in-post, adj-rib-in-pre and local-rib. </t>
            <t>An empty configuration or absence of a RIB-type container disables route-messages for it. Operators can also disable the route monitoring messages for each RIB explicitly by marking the "enabled" leaf as False. The next section provides an example of this, together with the address family configurations.</t>
          </section>
          <section title="Address families">
            <t>Address families are configured explicitly within each RIB type using a list. The key is of type `afi-safi-type` without any further constraint.</t>
            <t>An empty configuration or absence of an address family disables route-messages for it. Operators can also disable the address-family route monitoring messages by marking the "enabled" leaf as False.</t>
            <t>The next examples show different ways of configuring RIB-Types and Address families. As in the previous sections, the example masks further configurations of address families with "Configuration X" to focus on the covered parts.</t>
            <t>
              <figure anchor="example_rib_af_one">
                <name>Example of configuring RIBs and address families.</name>
                <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_example_rib_af_one.xml)
           ]]></sourcecode>
              </figure>
            </t>
            <t>Example <xref target="example_rib_af_one"/> expands the examples of previous sections with RIB-Type and address families configurations. The expected result of the previous configuration would be:</t>
            <t>
              <ul spacing="compact">
                <li>For the global network instance, adj-rib-in-pre and adj-rib-in-post RIBs are enabled. In each of them IPv4 and IPv6 address families are configured. The configuration can be the same or not, depending on the requirements of the operators. Any other RIB and address families are disabled.</li>
                <li>Network instance "network-instance-one"  is disabled, meaning that route monitoring messages are disabled for that network instance.</li>
                <li>Network instance "network-instance-two" has adj-rib-out-post enabled, but only address family ipv4-unicast is configured. The ipv6-unicast will not be configured for this instance.</li>
                <li>For all other network instances, adj-rib-in-pre with IPv4 and IPv6 address families are configured, thanks to the configuration of all-ni</li>
              </ul>
            </t>
            <t>If an operator only wants to configure the IPv4/IPv6 of adj-rib-in-pre for the global instance, the configuration in <xref target="example_rib_af_two"/> plus the peer configuration (coming in next section) will be enough. Note again that even if the configuration of both address families is the same, they must be explicitly configured for each of them.</t>
            <t>
              <figure anchor="example_rib_af_two">
                <name>Example of configuring RIBs and address families.</name>
                <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_example_rib_af_two.xml)
           ]]></sourcecode>
              </figure>
            </t>
          </section>
          <section title="Peers">
            <t>For adj-RIB-in and adj-RIB-out, both pre and post, the ietf-bmp module requires the selection of peer RIBs that will be transmitted to the monitoring station. The local-rib does not include this container.</t>
            <t>Peers can be configured using different "selectors", which can be one of the following: </t>
            <t>
              <ul spacing="compact">
                <li>An individual peer, using a remote address. For the configuration under the "/bmp" tree, the module  does not check if the remote address exists, that would be a responsibility of the device.</li>
                <li>Peergroups.</li>
                <li>A group of peers matching a BGP type. i.e. eBGP peers. </li>
                <li>One or more peers defined by a `peer-types` identity. The ietf-bmp module currently provides the `all-peers` identity, which selects all peers. For simple cases, this is the value that would normally be considered.</li>
              </ul>
            </t>
            <t>Peers MUST be selected (configured) by at most a single instance of the peers list. For the included keys in the "ietf-bmp" model, the process to select which instance to use is as follows:</t>
            <t>
              <ul spacing="compact">
                <li>If there is a peer address matching the peer, it SHOULD be configured using that instance.</li>
                <li>If the peer matches a peergroup, it SHOULD be configured using the peer-group configuration.</li>
                <li>If the peer is of any BGP type listed in the peer list, it SHOULD be configured using this instance.</li>
                <li>If there is a peer instance identified with the `all-peers`, it SHOULD be configured using this instance.</li>
                <li>Finally, if no instance covers the peer, route monitoring messages from this peer SHOULD NOT be transmitted to the monitoring station.</li>
              </ul>
            </t>
            <t>An empty configuration of a peer type disables route-messages for it. Operators can also disable the address-family route monitoring messages by marking the "enabled" leaf as False.</t>
            <t>Note that if an operator only wants the information of a few peers, it can enable them individually  using their id. If no other configuration exists, only the messages from those enabled peers will be transmitted to the monitoring station.</t>
            <t>Any additional peer-types identity created SHOULD describe how to unambiguously select a peer when there are conflicting options (multiple options covering the peer).</t>
            <t>Section <xref target="full_examples_sec"/> provides examples of the peers configuration.</t>
          </section>
          <section title="Filtering route-monitoring messages">
            <t>The local RIB, and the peer containers within the rest of rib types, include a filter container. This container includes mechanisms to filter route-monitoring messages for the specific RIB.</t>
            <t>The policy-filter can include a routing policy that, if existing, is applied to the outgoing updates to the monitoring station, and would serve as a granular way of filtering the messages that the monitoring station receives. </t>
            <t>Note that the policy-filter contains an `accept-route` default export policy. An operator can change it to a reject-route, if required.</t>
            <t>The policies created with the routing-policy can perform a large variety of actions on routes, and can filter them based on multiple characteristics. For the consistency of the data in the monitoring station, the routing policies actions MUST be restricted to accepting or rejecting routes. Furthermore, the conditions SHOULD only match prefix sets.</t>
          </section>
          <section title="Full examples of Route monitoring configurations" anchor="full_examples_sec">
            <section title="Example one - simple configuration">
              <t>In the example configuration from  <xref target="full_example_one"/>, address families IPv6 and IPv4 are configured to send all peers from the global network instance. This is an example of a simple configuration</t>
              <t>
                <figure anchor="full_example_one">
                  <name>Enabling Route monitoring for all peers in the global network instance; IPv4/IPv6 Address families,  in the adj-rib-in-pre RIB.</name>
                  <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_full_example_one.xml)
           ]]></sourcecode>
                </figure>
              </t>
            </section>
            <section title="Example two - policy list example">
              <t>In the example in <xref target="full_example_two"/>, the global network instance enables the adj-rib-in-pre. In this RIB, the IPv4 unicast address family is configured for all external peers. The example assumes peer 198.51.100.1 is external, but its BGP configuration is not shown in the snippet.  Peer 198.51.100.1, however, has a specific configuration: it announces everything but prefixes matching the test_policy list. Note that there is a default accept-route default policy in the model.</t>
              <t>
                <figure anchor="full_example_two">
                  <name>Configuring address families differently for the global network instance</name>
                  <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_full_example_two.xml)
           ]]></sourcecode>
                </figure>
              </t>
            </section>
            <section title="Example three - specific network instance configuration">
              <t>In the example from <xref target="full_example_three"/>, all network instances have adj-rib-in-pre with  IPv6  and IPv4  configured receiving all peers. network-instance-one is disabled, and network-instance-two is announcing only the local-rib/IPv4 unicast routes.</t>
              <t>
                <figure anchor="full_example_three">
                  <name>Applying a general configuration to all network instances, except of two, which are configured specifically.</name>
                  <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_full_example_three.xml)
           ]]></sourcecode>
                </figure>
              </t>
            </section>
            <section title="Example four - Enabling just a few peers in global">
              <t>In the example from <xref target="full_example_four"/>, the device is configured to send to the monitoring_station_one monitoring station only the route monitoring messages for IPv4 and IPv6 from peers "198.51.100.1" and "198.51.100.2" in the adj-rib-in-pre policy.</t>
              <t>
                <figure anchor="full_example_four">
                  <name>Sending just BGP messages from adj-rib-in-pre from 2 peers</name>
                  <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_full_example_four.xml)
           ]]></sourcecode>
                </figure>
              </t>
            </section>
          </section>
        </section>
        <!-- End of BMP route monitoring-->
      </section>
      <!-- End of BMP data-->
      <section title="Session stats" anchor="session_stats_section">
        <t>The non-configurable container "session-stats" includes various metrics for the session with the monitoring station.
            <xref target="tree_session_stats"/> shows the structure of the session-stats container.</t>
        <figure anchor="tree_session_stats">
          <name>Structure of the session-stats container</name>
          <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(trees/tree-session-stats.txt)
]]></sourcecode>
        </figure>
      </section>
      <section title="Actions" anchor="actions_section">
        <t>The ietf-bmp module provides actions to reset BMP sessions and counters.
            <xref target="tree_actions"/> shows the structure of the actions container.</t>
        <figure anchor="tree_actions">
          <name>Structure of the actions container</name>
          <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(trees/tree-actions.txt)
]]></sourcecode>
        </figure>
        <t>The "session-reset" action resets a session with a monitoring station.
            The "session-counter-reset" action resets the counters in the session-stats container.</t>
      </section>
    </section>
    <!-- End of Model description -->
    <section title="Implementation guidelines" anchor="implementation_guidelines">
      <t>To facilitate implementation, the specification is divided into two
            distinct parts. The core module (ietf-bmp) is designed to configure
            BMP without deep dependencies to other modules. This
            is an attempt to facilitate the implementation by vendors.</t>
      <t>Additionally, a supplementary module,
            ietf-bmp-tcp-dependencies,
            enhances the data model's functionality but relies on the IETF TCP
            YANG modules. This module can be adopted by implementations that
            support the dependency.</t>
    </section>
    <section title="YANG Modules" anchor="model-content">
      <section title="TCP dependencies for BMP YANG Module" anchor="ietf-bmp-tcp-dependencies.yang">
        <t>The ietf-bmp-tcp-dependencies module imports ietf-bmp from this document,
            ietf-tcp from <xref target="RFC9648"/>, ietf-tcp-common from
            <xref target="RFC9643"/>, and ietf-key-chain from <xref target="RFC8177"/>.</t>
        <figure>
          <sourcecode><![CDATA[
<CODE BEGINS> file "ietf-bmp-tcp-dependencies@2025-01-02.yang"
INSERT_TEXT_FROM_FILE(ietf-bmp-tcp-dependencies.yang)
<CODE ENDS>
            ]]></sourcecode>
        </figure>
      </section>
    </section>
    <section title="Implementation Status" anchor="implementation-status">
       <t>Note to the RFC-Editor: Please remove this section before publishing.</t>
       <section title="Arrcus ArcOS" anchor="arrcus-arcos">
           <t>Arrcus implemented the entire YANG module, except the optional tcp: statements (from RFC9648 and draft-ietf-grow-bmp-tcp-ao),
        which appear in version 8.</t>
        </section>
    </section>
    <section title="Security Considerations" anchor="security-considerations">
    <section title="Security Considerations for ietf-bmp module" anchor="security-considerations-main">
        <t>This section is modeled after the template described in Section
            3.7.1 of <xref target="RFC9907"/>.</t>

        <t> The ietf-bmp YANG module defines a data model that is designed to be
            accessed via YANG-based management protocols, such as NETCONF
        <xref target="RFC6241"/> and RESTCONF <xref target="RFC8040"/>. These YANG-based management
            protocols (1) have to use a secure transport layer (e.g., Secure
            Shell (SSH) <xref target="RFC4252"/>, TLS <xref target="RFC8446"/>,
            and QUIC <xref target="RFC9000"/>) and (2) have to use mutual
            authentication.
        </t>

        <t> The Network Configuration Access Control Model (NACM) <xref target="RFC8341"/>
            provides the means to restrict access for particular NETCONF or
            RESTCONF users to a preconfigured subset of all available NETCONF
            or RESTCONF protocol operations and content.
        </t>

        <t>
            There are a number of data nodes defined in this YANG module that
            are writable/creatable/deletable (i.e., "config true", which is the
            default). All writable data nodes are likely to be
            sensitive or vulnerable in some network environments. Write
            operations (e.g., edit-config) and delete operations to these data
            nodes without proper protection or authentication can have a
            negative effect on network operations. 
            There are no particularly sensitive writable data nodes.
        </t>
        <t>
            Some of the readable data nodes in this YANG module may be
            considered sensitive or vulnerable in some network environments. It
            is thus important to control read access (e.g., via get,
                get-config, or notification) to these data nodes. 
            There are no particularly sensitive readable data nodes.
        </t>
        <t>
            Some of the RPC or action operations in this YANG module may be
            considered sensitive or vulnerable in some network environments. It
            is thus important to control access to these operations.
            Specifically, the following operations have particular
            sensitivities/ vulnerabilities:
            The session-reset action can demand a considerable amount of resources
            from network elements.
            The session-counter-reset action can reset BMP session counters,
            which can affect operational visibility and troubleshooting.
            These actions SHOULD thus be protected from unauthorized access.
         </t>
    </section>
    <section title="Security Considerations for ietf-bmp-tcp-dependencies module" anchor="security-considerations-tcp">
        <t>This section is modeled after the template described in Section
            3.7.1 of <xref target="RFC9907"/>.</t>

        <t> The ietf-bmp-tcp-dependencies YANG module defines a data model that is designed to be
            accessed via YANG-based management protocols, such as NETCONF
        <xref target="RFC6241"/> and RESTCONF <xref target="RFC8040"/>. These YANG-based management
            protocols (1) have to use a secure transport layer (e.g., Secure
            Shell (SSH) <xref target="RFC4252"/>, TLS <xref target="RFC8446"/>,
            and QUIC <xref target="RFC9000"/>) and (2) have to use mutual
            authentication.
        </t>

        <t> The Network Configuration Access Control Model (NACM) <xref target="RFC8341"/>
            provides the means to restrict access for particular NETCONF or
            RESTCONF users to a preconfigured subset of all available NETCONF
            or RESTCONF protocol operations and content.
        </t>

        <t>
            There are a number of data nodes defined in this YANG module that
            are writable/creatable/deletable (i.e., "config true", which is the
            default). All writable data nodes are likely to be
            sensitive or vulnerable in some network environments. Write
            operations (e.g., edit-config) and delete operations to these data
            nodes without proper protection or authentication can have a
            negative effect on network operations. 
            There are no particularly sensitive writable data nodes.
        </t>
        <t>
            Some of the readable data nodes in this YANG module may be
            considered sensitive or vulnerable in some network environments. It
            is thus important to control read access (e.g., via get,
                get-config, or notification) to these data nodes. 
            There are no particularly sensitive readable data nodes.
        </t>
         <t>
             This YANG module uses groupings from other YANG modules that
             define nodes that may be considered sensitive or vulnerable in
             network environments. Refer to the Security Considerations of
             <xref target="RFC9643"/>, <xref target="RFC9648"/>, and
             <xref target="RFC8177"/> for information as to which nodes may be
             considered sensitive or vulnerable in network environments.
         </t>
    </section>
    </section>
    <section title="IANA Considerations" anchor="iana-considerations">
      <t>IANA is requested to register the following URIs in the "ns"
   registry within the "IETF XML Registry" group <xref target="RFC3688"/>:</t>
      <sourcecode type="text"><![CDATA[
   URI: urn:ietf:params:xml:ns:yang:ietf-bmp
   Registrant Contact:  The IESG.
   XML: N/A; the requested URI is an XML namespace.
        ]]></sourcecode>
      <sourcecode type="text"><![CDATA[
   URI: urn:ietf:params:xml:ns:yang:ietf-bmp-tcp-dependencies
   Registrant Contact:  The IESG.
   XML: N/A; the requested URI is an XML namespace.
        ]]></sourcecode>

      <t>IANA is requested to register the following YANG modules in the "YANG
            Module Names" registry <xref target="RFC6020"/> within the "YANG Parameters"
            registry group.</t>
      <sourcecode type="text"><![CDATA[
   Name: ietf-bmp
   Maintained by IANA? N
   Namespace: urn:ietf:params:xml:ns:yang:ietf-bmp
   Prefix: bmp
   Reference: RFC XXXX
        ]]></sourcecode>
      <sourcecode type="text"><![CDATA[
   Name: ietf-bmp-tcp-dependencies
   Maintained by IANA?  N
   Namespace: urn:ietf:params:xml:ns:yang:ietf-bmp-tcp-dependencies
   Prefix: bmp-tcp
   Reference: RFC XXXX
        ]]></sourcecode>
    </section>
  </middle>
  <back>
    <references title="Normative References">
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.2119.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.2385.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.3688.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.4252.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.4271.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.4760.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.5065.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.5925.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.6020.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.6241.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.6991.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.7854.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8671.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8040.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8174.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8341.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8343.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8446.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8529.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8792.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9000.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9067.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9069.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9293.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8177.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9643.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9648.xml"/>
    </references>
    <references title="Informative References">
    <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8340.xml"/>
    <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9907.xml"/>
    </references>
    <!--
        -->
    <?rfc needLines="100"?>
    <!--   
    <section title="Changes between revisions">
      </t>   
      <t>v00 - v01
        <list style="symbols">
          <t>Placeholder: xxx</t>
          <t>Placeholder: yyy</t>
        </list>
      </t>
    </section>    
  -->
    <section title="BMP YANG module tree" anchor="ietf-bmp-tree-view">
      <t>
        The following tree diagram provides an overview of the base ietf-bmp.yang
            module. It also includes the tree for the ietf-bmp-tcp-dependencies.yang module
            that adds some options to the data model in case the implementation supports it and its prerequisites.
        </t>
        <t>The tree diagrams follow the conventions in <xref target="RFC8340"/>.</t>
      <t>
        <figure>
          <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(ietf-bmp-trees.txt)
           ]]></sourcecode>
        </figure>
      </t>
    </section>
    <section title="Base BMP YANG Module" anchor="ietf-bmp">
      <t>The ietf-bmp module imports ietf-yang-types and ietf-inet-types from
          <xref target="RFC6991"/>, ietf-netconf-acm from <xref target="RFC8341"/>,
          ietf-interfaces from <xref target="RFC8343"/>, ietf-network-instance from
          <xref target="RFC8529"/>, and ietf-routing-policy from
          <xref target="RFC9067"/>.</t>
      <figure>
        <sourcecode><![CDATA[
<CODE BEGINS> file "ietf-bmp@2025-01-02.yang"
INSERT_TEXT_FROM_FILE(ietf-bmp.yang)
<CODE ENDS>
            ]]></sourcecode>
      </figure>
    </section>
    <section title="Examples">
      <t>This section shows some examples of BMP configuration using the
            data model.</t>
      <section title="Example one">
        <t>In this example, the device connects to a monitoring station
            using an active connection. 
            The devices sends route monitoring messages for the global
            instance, the adj-rib-in-pre RIB, the IPv4/IPv6 address family,
            and external peers.
            </t>
        <t>
          <figure anchor="example_one">
            <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_example_one.xml)
]]></sourcecode>
          </figure>
        </t>
      </section>
      <section title="Example two">
        <t>In the next example, the device connects to a monitoring station
            using a passive connection, over the network-instance monitoring.
            The configuration of route monitoring messages is more complex than
            in the previous example.  It shows how to combine the configuration
            of general identities of network instances and peers (e.g.
            all-ni for NI, external for peers), and individual
            configurations to support a more complex requirement. This is what
                the example expects to configure:</t>
        <t>
          <list style="symbols">
            <t> For the global network instance,
            the device sends updates for adj-rib-in-pre, address families IPv4
            and IPv6. It sends updates for all external peers except peer
            198.51.100.11, which is disabled.</t>
            <t>Network instance monitoring is disabled for route monitoring messages. </t>
            <t>For the rest of network
            instances, route monitoring messages are enabled from adj-rib-in-pre, address
                families IPv4/IPv6, and for all peers.</t>
          </list>
        </t>
        <t>
          <figure anchor="example_two">
            <sourcecode><![CDATA[
INSERT_TEXT_FROM_FILE(clean_examples/figure_example_two.xml)
]]></sourcecode>
          </figure>
        </t>
      </section>
    </section>
    <section title="Acknowledgements" numbered="no">
      <t>
          The authors would like to thank Yimin Shen, Jeff Haas, Pierre Vander
            Vorst, Tom Petch for their review and feedback, and Ijsbrand Wijnands
            for the first implementation.
        </t>
    </section>
  </back>
</rfc>
<!-- Local Variables: -->
<!-- fill-column:72 -->
<!-- End: -->
